@model IEnumerable<TMC.DATA.TbCompras>

@{
    ViewBag.Title = "Search";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Registro de Compras</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <thead class="thead-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IDCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IDUsuario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IDServicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.precioFinal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fecha)
            </th>
            <th></th>
        </tr>
    </thead>


    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IDCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IDUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IDServicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.precioFinal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fecha)
            </td>
            <td>
                @Html.ActionLink(" ", "Edit", new { id = item.IDCompra }, new { @class = "fas fa-edit text-reset", @title = "Editar Catalogo" }) |
                @Html.ActionLink(" ", "Detail", new { id = item.IDCompra }, new { @class = "fas fa-info-circle text-reset", @title = "Detallar Catalogo" }) |
                @Html.ActionLink(" ", "Delete", new { id = item.IDCompra }, new { @class = "fas fa-trash-alt text-reset", @title = "Eliminar Catalogo" })
        </tr>
    }

</table>
<script src="https://kit.fontawesome.com/065c8b2d38.js" crossorigin="anonymous"></script>
